import os
from setuptools import setup, find_packages

setup(
    name="jbdiff",
    py_modules=["jbdiff"],
    version="1.0",
    description="",
    author="Jeff Sontag",
    packages=find_packages(),
    install_requires=[
    ],
    include_package_data=True
)